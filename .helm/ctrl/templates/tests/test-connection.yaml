apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ctrl.fullname" . }}-test-connection"
  labels:
    {{- include "ctrl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: alpine/curl
      command: ['sh','-c']
      args: ['curl ctrl && curl admin && curl --retry 5 --retry-connrefused backend']
  restartPolicy: Never
