apiVersion: v1
kind: Service
metadata:
  name: admin-client
  labels:
    {{- include "ctrl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: ADMINCLIENT
